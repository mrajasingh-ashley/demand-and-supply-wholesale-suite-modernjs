import { Typography } from 'antd';
const { Title } = Typography;
export default function PlanningMasterControl() {
  return (
    <div style={{ padding: '24px' }}>
      <Title level={2}>Planning Master Control</Title>
      <p>This page will be used for the planning master control panel.</p>
    </div>
  );
}
