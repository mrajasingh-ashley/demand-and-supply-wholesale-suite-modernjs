apiVersion: v1
kind: Service
metadata:
  name: demand-planning-web
  namespace: demand-planning
  labels:
    app: demand-planning-web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: demand-planning-web
