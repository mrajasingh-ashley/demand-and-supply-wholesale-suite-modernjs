apiVersion: v1
kind: Service
metadata:
  name: demand-supply-frontend
  namespace: demand-planning
  labels:
    app: demand-supply-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: demand-supply-frontend
